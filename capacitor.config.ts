import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manuelfqc.dev',
  appName: 'CoinkAngularIonic',
  webDir: 'dist/coink-angular-ionic',
  server: {
    androidScheme: 'https'
  }
};

export default config;
